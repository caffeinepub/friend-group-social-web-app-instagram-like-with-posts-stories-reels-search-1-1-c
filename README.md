## Friend-group social web app (Instagram-like) with posts, stories, reels, search, 1:1 chat, games, music library, and study tools

To run app build and run docker image: `docker build -t app . docker run -it --network host app`
